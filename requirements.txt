# HAREN-CTC — Python 3.12 tested. Versions below match the reference environment.
# For a CUDA build of PyTorch, install torch/torchaudio from the official index, e.g.:
#   pip install torch==2.7.0 torchaudio==2.7.0 --index-url https://download.pytorch.org/whl/cu126
torch==2.7.0
torchaudio==2.7.0
transformers==4.57.6
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.0
scipy>=1.11
tqdm>=4.66
pydub>=0.25.1          # data preprocessing (requires ffmpeg on PATH)
